/*
For custom network connections, for example client->broker or broker->broker, defines a mechanism
for multiplexing request messages and responses over the connection so that it will work efficiently
over a high latency connection
*/
